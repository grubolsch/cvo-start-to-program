function greeting(gender, name) {
    console.log(gender, name);

    //start solution
    if(gender === "man") {
        return "Meneer " + name;
    }

    return "Mevrouw " + name;
    //end

    return 'Hier komt jouw resultaat';
}
