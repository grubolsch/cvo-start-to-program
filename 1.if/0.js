function greeting(age) {
    // Hier komt jouw code
    console.log(age);

    if(age >= 18) {
        return 'Welkom!'
    }

    return 'Verboden toegang voor minderjarigen.';

    return 'Hier komt jouw resultaat';
}
